name: CI

on:
  push:
    branches: [main]
  pull_request:

# A superseded run tells you nothing. Cancel it.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  # Every step below is a `just` recipe, and `just ci` runs the same list in the
  # same order. If you add a step here that is not a recipe, that is the bug
  # (SPEC §13.3).
  check:
    name: check (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    steps:
      - uses: actions/checkout@v7

      # No toolchain action: rust-toolchain.toml pins the compiler and rustup
      # honours it, so CI cannot drift from what developers run locally.
      - run: rustup toolchain install

      - uses: Swatinem/rust-cache@v2

      - uses: taiki-e/install-action@v2
        with:
          tool: just,cargo-nextest,cargo-deny

      - name: Formatting
        run: just fmt-check

      - name: Clippy
        run: just lint

      - name: Docs
        run: just doc

      - name: Tests
        run: just test

      - name: Licences and advisories
        run: just deny

      - name: OpenAPI drift
        run: just openapi-check

      - name: Web UI
        run: just web-build

  coverage:
    name: coverage
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - run: rustup toolchain install
      - uses: Swatinem/rust-cache@v2
      - uses: taiki-e/install-action@v2
        with:
          tool: just,cargo-nextest,cargo-llvm-cov

      # The 85% floor is live as of M1 (SPEC §13.2, §14). Coverage is a smoke
      # detector, not a target: if this fails, look at what is untested before
      # reaching for the number.
      - name: Coverage
        run: just cov-gate

      - name: Summary
        if: always()
        run: |
          {
            echo '### Coverage'
            echo
            echo '```'
            cargo llvm-cov report --summary-only \
              --ignore-filename-regex '(tests/|crates/hivemind-cli/)' || true
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"

      - uses: actions/upload-artifact@v7
        if: always()
        with:
          name: lcov
          path: lcov.info
          if-no-files-found: ignore
